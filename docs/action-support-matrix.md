# Workflow action support matrix

This matrix documents the current on-disk implementation under `src/SPNet.Workflow.WfSerializer/` and `src/SPNet.Workflow.Publisher.Csom/`. Visible editor tabs may reference older `Core`, `Cli`, or `SharePoint` projects, but those projects are not present in this workspace snapshot or in `SPNet.slnx`.

| Action name | Aliases | YAML model | Builder / YAML-to-XAML status | Export status | Sample coverage | Test target | Validation status | Risk | Caveats |
|---|---|---|---|---|---|---|---|---|---|
| `calc` | none | `CalcActionYaml` | Supported; emits SharePoint `Calc`. | Partial structural export with placeholder expressions. | `samples/workflow.example.yml`, `samples/workflow.expanded-actions.yml` | Alias/model/required `to`. | Requires `to`. | Low | Target variable is inferred as `Double` if omitted from variables. |
| `assign` | `setVariable` | `AssignActionYaml` | Supported; emits WF `Assign<T>`. | Not exported. | `samples/workflow.assign.yml`, lookup/control-flow samples | Alias/model/required `to`; nested lookup safety. | Requires `to`; build requires declared variable. | Low | Use for safe nested lookup expression shapes. |
| `replaceString` | `stringReplace` | `StringReplaceActionYaml` | Supported; emits WF `Assign<String>` with a Visual Basic string `Replace` expression. | Partial structural export recognizes matching string `Assign` expressions with placeholders. | `samples/workflow.string-actions.yml` | Alias/model/required `to`; export recognition. | Requires `to`; target variable is inferred as `String` if omitted. | Medium | Literal and variable operands are supported for build; nested expression operands are intentionally not expanded into Visual Basic. |
| `substring` | `substringString` | `StringSubstringActionYaml` | Supported; emits WF `Assign<String>` with a Visual Basic string `Substring` expression. | Partial structural export recognizes matching string `Assign` expressions with placeholders. | `samples/workflow.string-actions.yml` | Alias/model/required `to`; export recognition. | Requires `to`; target variable is inferred as `String` if omitted. | Medium | Uses zero-based .NET substring indexes; optional `length` omits the second `Substring` argument. |
| `trimString` | `stringTrim` | `StringTrimActionYaml` | Supported; emits WF `Assign<String>` with a Visual Basic string `Trim` expression. | Partial structural export recognizes matching string `Assign` expressions with placeholders. | `samples/workflow.string-actions.yml` | Alias/model/required `to`; export recognition. | Requires `to`; target variable is inferred as `String` if omitted. | Medium | Trims leading/trailing whitespace only. |
| `writeHistory` | none | `WriteHistoryActionYaml` | Supported; emits SharePoint `WriteToHistory`. | Partial structural export with placeholder expression. | Multiple samples | Model smoke. | Common validation only. | Low | Prefer scalar/string values or explicit `toString`. |
| `setStatus` | none | `SetStatusActionYaml` | Supported; emits SharePoint `SetWorkflowStatus`. | Partial structural export with status. | `samples/workflow.control-flow.yml`, expanded samples | Model smoke. | Common validation only. | Low | Also inserted by metadata normalization for stage header. |
| `comment` | none | `CommentActionYaml` | Supported; emits SharePoint `Comment`. | Structural export recognition added when `Comment` is present in reference XAML. | `samples/workflow.expanded-actions.yml` | Model smoke. | Common validation only. | Low | Designer annotation, not runtime history logging. |
| `delayFor` | none | `DelayForActionYaml` | Supported; emits SharePoint `DelayFor`. | Structural export recognition added when `DelayFor` is present in reference XAML. | `samples/workflow.expanded-actions.yml` | Model smoke. | Common validation only. | Medium | Runtime timing not covered by automated tests. |
| `delayUntil` | none | `DelayUntilActionYaml` | Supported; emits SharePoint `DelayUntil`. | Structural export recognition added when `DelayUntil` is present in reference XAML. | `samples/workflow.expanded-actions.yml` | Model smoke. | Common validation only. | Medium | Prefer explicit date literals or declared `DateTime` variables. |
| `while` | `loop` | `WhileActionYaml` | Supported; emits WF `While`. | Not exported. | `samples/workflow.control-flow.yml` | Alias/model/nested validation. | Requires non-empty condition type/operator. | Medium | Numeric comparisons only. |
| `if` | none | `IfActionYaml` | Supported; emits WF `If`. | Not exported. | `samples/workflow.control-flow.yml` | Model/nested validation. | Requires non-empty condition type/operator. | Medium | Numeric comparisons only. |
| `lookupWorkflowContext` expression | `lookupContextProperty` | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Supported only as nested expression. | Not exported. | `samples/workflow.lookup.yml`, HTTP samples | Alias/model; top-level negative test. | Nested expression requires `propertyName`; top-level action rejected. | Medium | Direct stage action is SPD-unsafe. |
| `getCurrentListId` expression | none | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Supported only as nested expression. | Not exported. | list samples | Top-level negative test. | Top-level action rejected. | Medium | Direct stage action is SPD-unsafe. |
| `getCurrentItemGuid` expression | none | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Not exported. | lookup/list samples | Top-level negative test. | Top-level action rejected. | Medium | Direct stage action is SPD-unsafe. |
| `setField` | none | `SetFieldActionYaml` | Supported; emits SharePoint `SetField`. | Structural export recognition added when `SetField` is present in reference XAML. | `samples/workflow.list-actions.yml` | Required `fieldName`. | Requires `fieldName`. | Medium | Mutates current item; no live fixture tests. |
| `createListItem` | none | `CreateListItemActionYaml` | Supported; emits SharePoint `CreateListItem`. | Structural export recognition added from checked-in lifecycle XAML; field keys retained, expressions placeholdered. | `samples/workflow.list-lifecycle.yml` | Required fields/list defaults plus reference export test. | Requires list id and non-empty fields. | High | Mutating action; Designer UI may show misleading red boxes. |
| `updateListItem` | none | `UpdateListItemActionYaml` | Supported; emits SharePoint `UpdateListItem`. | Structural export recognition added from checked-in lifecycle/diagnostics XAML; field keys retained, expressions placeholdered. | `samples/workflow.list-lifecycle.yml` | Required identity/fields plus reference export test. | Requires list id, identity, fields. | High | Mutating action; automated tests do not touch SharePoint. |
| `deleteListItem` | none | `DeleteListItemActionYaml` | Supported; emits SharePoint `DeleteListItem`. | Structural export recognition added from checked-in diagnostics XAML; identity expressions placeholdered. | README shape only; intentionally omitted from safe sample. | Required identity plus reference export test. | Requires list id and identity. | High | Destructive; no live lifecycle fixture. |
| `lookupListItemStringProperty` expression | `lookupSPListItemStringProperty` | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Supported only as nested expression. | Not exported. | `samples/workflow.list-item-lookup.yml` | Alias/model; top-level negative test. | Nested expression requires property/field and identity; top-level action rejected. | High | Direct stage action can be invisible in Designer and crash property editing. |
| `lookupListItemIntProperty` | `lookupSPListItemIntProperty` | `LookupListItemIntPropertyActionYaml` | YAML shape only; build fails if proxy type missing; top-level rejected. | Not exported. | none | Top-level negative test. | Top-level action rejected. | High | Tested cache lacks `LookupSPListItemIntProperty`; do not publish as visible action. |
| `callHttpWebService` | `callHttp`, `http` | `CallHttpWebServiceActionYaml` | Supported; emits SharePoint `CallHTTPWebService`. | Structural export recognition added when `CallHTTPWebService` is present in reference XAML. | `samples/workflow.http.yml`, complex samples | Alias/model/method validation. | Requires at least one response target; literal method allow-list. | Medium | No external HTTP call in automated tests. |
| `sendEmail` | `email` | `SendEmailActionYaml` | Supported; emits SharePoint `Email`. | Structural export recognition added from checked-in email/reference XAML; recipients placeholdered. | `samples/workflow.email.yml`, complex samples | Alias/model/required `to`. | Requires `to`. | High | Can notify real users; samples use safe placeholder recipients. |
| `singleTask` | `task` | `SingleTaskActionYaml` | Supported; emits bounded SharePoint `SingleTask`. | Structural export recognition added when `SingleTask` is present in reference XAML; assignee/title/body are retained when simple attributes. | `samples/workflow.task.yml` | Alias/model/required assignee/title plus structural export recognition. | Requires `assignedTo` and `title`. | High | Defaults waive emails; review assignees before live publish. |
| `lookupRestPropertyName` | `lookupSPListItemPropertyNameInREST` | `LookupRestPropertyNameActionYaml` | Supported; emits SharePoint REST property-name lookup. | Not exported. | HTTP/REST samples | Alias/model/required `to`. | Requires `to`. | Medium | Requires proxy metadata at build time. |
| `getDynamicValueProperty` | `getDictionaryItem`, `getDictionaryValue`, `getResponseProperty` | `GetDynamicValuePropertyActionYaml` | Supported for string extraction from HTTP `DynamicValue`. | Not exported. | HTTP/REST samples | Alias/model/required source/to. | Requires `source` and `to`; build validates variable types. | Medium | Currently string targets only. |

## Export parity notes

`ExportWorkflowYaml` is a lightweight structural export for diagnostics, not a full XAML-to-YAML round trip. It recognizes direct SharePoint child activities that are already supported by YAML-to-XAML and now has reference-backed recognition for common generated/reference XAML shapes including `Comment`, delays, `SetField`, list lifecycle actions, `Email`, `CallHTTPWebService`, `SingleTask`, and string-manipulation `Assign` expressions for `Replace`, `Substring`, and `Trim`. Expressions, recipients, list IDs, identities, and string operands are intentionally placeholdered unless represented as simple attributes or discoverable argument names. Most generated workflows are wrapped in SharePoint Designer stage metadata, nested WF `InArgument` expressions, and proxy-specific activity shapes; adding robust export for the full action surface requires either WF deserialization with the local WebsiteCache proxy assemblies or more detailed proxy metadata than this repository can safely assume. Export warnings remain explicit so partial diagnostic output is not mistaken for authoritative round-trip YAML.

## Adding a new action checklist

Keep action additions narrow and make each supported action explicit across the YAML, build, export, and test surfaces:

- Add or extend the YAML model in `YamlWorkflowModel.cs`, including a stable canonical `Type` value and any safe aliases in `WorkflowActionYamlTypeConverter`.
- Add validation near the model for required fields, safe top-level versus nested-expression usage, and any allow-lists that prevent unsupported Designer/runtime shapes.
- Update `WorkflowVariableTypeInferer` when the action writes to workflow variables or requires implicit infrastructure variables.
- Implement the activity builder in the appropriate file under `Actions/`, reusing `WorkflowExpressionBuilder` and preserving existing expression behavior.
- Register the action model type in `WfActivityBuilderSerializer.CreateActionBuilders()` so dispatch is explicit and discoverable.
- Decide whether the action is valid as a nested expression, a stage action, or both; document and test the expression behavior.
- Update `WorkflowYamlExporter` only if structural export can recognize the action safely; otherwise mark export as unsupported in this matrix.
- Add or update tests for aliases/model conversion, validation failures, build-dispatch registration, variable inference, and export recognition when applicable.
- Add a safe sample YAML file or extend an existing sample only when the action can be demonstrated without live side effects.
- Add or update the support-matrix row with aliases, builder status, export status, sample/test coverage, validation status, risk, and caveats.

## Repository structure note

The solution file `SPNet.slnx` includes only the serializer and CSOM publisher projects, plus the test project added under `tests/`. If IDE state shows `src/SPNet.Workflow.Core/`, `src/SPNet.Workflow.Cli/`, or `src/SPNet.Workflow.SharePoint/`, those are stale visible tabs or files from another workspace state and are not part of this repository snapshot.
