# Workflow action support matrix

This matrix documents the current on-disk implementation under `src/SPNet.Workflow.WfSerializer/` and `src/SPNet.Workflow.Publisher.Csom/`. Visible editor tabs may reference older `Core`, `Cli`, or `SharePoint` projects, but those projects are not present in this workspace snapshot or in `SPNet.slnx`.

Raw WF language expression activities are forbidden in generated/published SharePoint workflow XAML. Do not emit `VisualBasicValue`, `VisualBasicReference`, `CSharpValue`, or `CSharpReference`: they require VB/C# compilation and are rejected by SharePoint Workflow Manager validation (a raw `Microsoft.CSharp.Activities.CSharpValue<TResult>` publish probe failed as an invalid type). Builders must prefer structured SharePoint Designer/Workflow Manager-safe nodes such as SharePoint proxy activities and `Microsoft.Activities.Expressions` proxy expression activities. Exporter recognition of raw VB expression text is legacy/downloaded-XAML compatibility only and must not be treated as permission to generate those nodes.

YAML remains the authoring source of truth. Build output is the generated XAML plus a generated `*.xaml.metadata.json` sidecar. That metadata JSON is the normal publish contract for workflow display name/technical name/description, target, start options, initiation settings, and form fields. Legacy `*.xaml.formfield.xml` is compatibility/inspection output and an explicit deprecated fallback only; it is not the normal YAML publish input.

Stage transitions are part of the supported workflow-structure surface rather than an action row. `stage.id`, `stage.transition.branches`, `transition.default.goto`, shorthand `transition.goto`, and terminal `goto: end` are supported. YAML-to-XAML builds stage graphs as `System.Activities.Statements.Flowchart` / `FlowStep` / `FlowDecision` object-model nodes, and serialization emits SharePoint Designer-visible stage metadata including the SPD end sentinel `4294967294`. See `docs/stage-transitions.md` and the samples `samples/workflow.stage-transitions.yml` and `samples/workflow.stage-flow-concepts.yml`.

## Support classification meanings

- **stable**: suitable for production use after normal SharePoint test-site validation; uses visible or well-understood Workflow Manager-safe activity shapes and avoids hidden activity reliance.
- **preview**: build support exists and narrow validation has passed, but production use needs extra review because the action may depend on exact SharePoint Designer metadata, runtime timing, external services, notification recipients, list side effects, or partial export support.
- **experimental**: available for controlled trials only; runtime/publish behavior may depend on hidden activities, `DynamicValue` shapes, or site-specific proxy behavior.
- **dev-only**: intended for local diagnostics, sample builds, and controlled developer experiments; do not use in production workflows unless separately validated and accepted by the owning site team.
- **unsupported**: documented shape or rejected model only; do not publish from YAML.

Production workflows should stay primarily in the **stable** set, add **preview** actions only after a live test-site publish/download/runtime check, and avoid **experimental** or **dev-only** actions for business-critical processes. SharePoint Workflow Manager has practical runtime limits around large activity graphs, variable/property counts, long loops, payload size, and external service latency. Use workflows for orchestration and light value shaping rather than heavy matrix-style computation, large in-workflow transformations, or broad data processing.

| Action name | Support | Aliases | YAML model | Builder / YAML-to-XAML status | Export status | Sample coverage | Test target | Validation status | Risk | Caveats |
|---|---|---|---|---|---|---|---|---|---|---|
| `calc` | stable | none | `CalcActionYaml` | Supported; emits SharePoint `Calc`. | Partial structural export with placeholder expressions. | `samples/workflow.example.yml`, `samples/workflow.expanded-actions.yml` | Alias/model/required `to`. | Requires `to`. | Low | Target variable is inferred as `Double` if omitted from variables. |
| `assign` | stable | `setVariable` | `AssignActionYaml` | Supported; emits WF `Assign<T>` with typed arguments or structured nested expression activities, not raw VB/C# language expression activities. | Structural export recognizes WF `Assign` targets and simple literal/variable/legacy VB expression values for import compatibility. | `samples/workflow.assign.yml`, lookup/control-flow samples | Alias/model/required `to`; nested lookup safety; export recognition. | Requires `to`; build requires declared variable. | Low | Export remains conservative for complex legacy VB expressions and emits scalar text rather than guessing unsafe nested shapes; generated XAML must not use raw VB/C# expression nodes. |
| `replaceString` | stable | `stringReplace` | `StringReplaceActionYaml` | Supported; emits WF `Assign<String>` with structured Microsoft.Activities `ReplaceString`, not raw `VisualBasicValue<T>`. | Structural export recognizes generated `ReplaceString` activity shapes and retains simple literal/variable operands; legacy VB text falls back to placeholders. | `samples/workflow.string-actions.yml` | Alias/model/required `to`; export recognition and operand reconstruction. | Requires `to`; target variable is inferred as `String` if omitted. | Medium | Literal and variable operands are supported for build; complex/proxy-specific operands still placeholder conservatively; avoid repeated full-body replacements over large text values. |
| `substring` | stable | `substringString` | `StringSubstringActionYaml` | Supported; emits WF `Assign<String>` with structured Microsoft.Activities `Substring`, not raw `VisualBasicValue<T>`. | Structural export recognizes generated `Substring` activity shapes and retains simple literal/variable operands; legacy VB text falls back to placeholders. | `samples/workflow.string-actions.yml` | Alias/model/required `to`; export recognition and operand reconstruction. | Requires `to`; target variable is inferred as `String` if omitted. | Medium | Uses zero-based .NET substring indexes; optional `length` omits the second `Substring` argument; validate bounds against real runtime values. |
| `trimString` | stable | `stringTrim` | `StringTrimActionYaml` | Supported; emits WF `Assign<String>` with structured Microsoft.Activities `Trim`, not raw `VisualBasicValue<T>`. | Structural export recognizes generated `Trim` activity shapes and retains simple literal/variable input; legacy VB text falls back to placeholders. | `samples/workflow.string-actions.yml` | Alias/model/required `to`; export recognition and operand reconstruction. | Requires `to`; target variable is inferred as `String` if omitted. | Medium | Trims leading/trailing whitespace only. |
| `writeHistory` | stable | none | `WriteHistoryActionYaml` | Supported; emits SharePoint `WriteToHistory`. | Partial structural export with placeholder expression. | Multiple samples | Model smoke. | Common validation only. | Low | Prefer scalar/string values or explicit `toString`; avoid high-volume loop logging. |
| `setStatus` | stable | none | `SetStatusActionYaml` | Supported; emits SharePoint `SetWorkflowStatus`. | Partial structural export with status. | `samples/workflow.control-flow.yml`, expanded samples | Model smoke. | Common validation only. | Low | Also inserted by metadata normalization for stage header. |
| `comment` | stable | none | `CommentActionYaml` | Supported; emits SharePoint `Comment`. | Structural export recognition added when `Comment` is present in reference XAML. | `samples/workflow.expanded-actions.yml` | Model smoke. | Common validation only. | Low | Designer annotation, not runtime history logging. |
| `delayFor` | preview | none | `DelayForActionYaml` | Supported; emits SharePoint `DelayFor`. | Structural export recognition added when `DelayFor` is present in reference XAML. | `samples/workflow.expanded-actions.yml` | Model smoke. | Common validation only. | Medium | Runtime timing not covered by automated tests; validate timer behavior in target Workflow Manager farm/tenant. |
| `delayUntil` | preview | none | `DelayUntilActionYaml` | Supported; emits SharePoint `DelayUntil`. | Structural export recognition added when `DelayUntil` is present in reference XAML. | `samples/workflow.expanded-actions.yml` | Model smoke. | Common validation only. | Medium | Prefer explicit date literals or declared `DateTime` variables; timezone handling must be checked against target site behavior. |
| `while` | preview | `loop` | `WhileActionYaml` | Supported; emits WF `While` with structured Boolean condition expressions. | Structural export recognizes WF `While`, preserves supported nested actions, reconstructs supported logical and comparison condition trees, preserves Designer `Id` as `designerId` where needed, and emits buildable placeholders when unavailable. | `samples/workflow.control-flow.yml`, `samples/workflow.conditionals-test.yml` | Alias/model/nested validation; export recognition and condition reconstruction. | Requires non-empty condition type/operator. | Medium | Supports nested `and`/`or`/`not`, numeric/string/Boolean/DateTime/DynamicValue comparisons, `parseDate`, and `parseDynamicValue`; Designer rendering depends on SPD-compatible metadata/wrappers; cap loop counts and nested actions to avoid Workflow Manager runtime limits. |
| `if` | preview | none | `IfActionYaml` | Supported; emits WF `If` with structured Boolean condition expressions. | Structural export recognizes WF `If`, preserves supported nested then/else actions, reconstructs supported logical and comparison condition trees, preserves Designer `Id` as `designerId` where needed, and emits buildable placeholders when unavailable. | `samples/workflow.control-flow.yml`, `samples/workflow.conditionals-test.yml`; `artifacts/ExampleConditionals*.yml` diagnostics | Model/nested validation; export recognition and condition reconstruction. | Requires non-empty condition type/operator. | Medium | Supports nested `and`/`or`/`not`, numeric/string/Boolean/DateTime/DynamicValue comparisons, `parseDate`, and `parseDynamicValue`; Designer can show `(insert a condition)` or blank RHS operands if metadata/wrappers/IDs are not preserved exactly. |
| `lookupWorkflowContext` expression | stable | `lookupContextProperty` | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Supported only as nested expression. | Partial nested expression export from simple recognizable VB call text and generated `LookupWorkflowContextProperty` shapes. | `samples/workflow.lookup.yml`, HTTP samples | Alias/model; top-level negative test; nested export reconstruction. | Nested expression requires `propertyName`; top-level action rejected. | Medium | Direct stage action is SPD-unsafe; nested expression use should remain inside visible stable/preview actions. |
| `getCurrentListId` expression | stable | none | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Supported only as nested expression. | Partial nested expression export from simple recognizable VB call text and generated `GetCurrentListId` shapes. | list samples | Top-level negative test; nested export reconstruction. | Top-level action rejected. | Medium | Direct stage action is SPD-unsafe; use nested in current-list/list-item arguments only. |
| `getCurrentItemGuid` expression | stable | none | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Supported as nested expression. | Partial nested expression export from simple recognizable VB call text and generated `GetCurrentItemGuid` shapes. | lookup/list samples | Top-level negative test. | Top-level action rejected. | Medium | Direct stage action is SPD-unsafe; use nested in current-item arguments only. |
| `setField` | preview | none | `SetFieldActionYaml` | Supported; emits SharePoint `SetField`. | Structural export recognition added when `SetField` is present in reference XAML. | `samples/workflow.list-actions.yml` | Required `fieldName`. | Requires `fieldName`. | Medium | Mutates current item; no live fixture tests; validate on a non-production list before use. |
| `createListItem` | preview | none | `CreateListItemActionYaml` | Supported; emits SharePoint `CreateListItem`. | Structural export recognition added from checked-in lifecycle XAML; field keys retained, expressions placeholdered. | `samples/workflow.list-lifecycle.yml` | Required fields/list defaults plus reference export test. | Requires list id and non-empty fields. | High | Mutating action; Designer UI may show misleading red boxes; runtime-validate target list fields and permissions. |
| `updateListItem` | preview | none | `UpdateListItemActionYaml` | Supported; emits SharePoint `UpdateListItem`. | Structural export recognition added from checked-in lifecycle/diagnostics XAML; field keys retained, expressions placeholdered. | `samples/workflow.list-lifecycle.yml` | Required identity/fields plus reference export test. | Requires list id, identity, fields. | High | Mutating action; automated tests do not touch SharePoint; runtime-validate identity selection to avoid changing the wrong item. |
| `deleteListItem` | preview | none | `DeleteListItemActionYaml` | Supported; emits SharePoint `DeleteListItem`. | Structural export recognition added from checked-in diagnostics XAML; identity expressions placeholdered. | README shape only; intentionally omitted from safe sample. | Required identity plus reference export test. | Requires list id and identity. | High | Destructive; no live lifecycle fixture; keep out of production until separately approved and tested with recoverable data. |
| `lookupListItemStringProperty` expression | preview | `lookupSPListItemStringProperty` | `ExpressionYaml`; top-level model exists only to reject unsafe usage. | Supported only as nested expression. | Partial nested expression export for recognizable generated `LookupSPListItemStringProperty` shapes with safe simple list/item/field operands. | `samples/workflow.list-item-lookup.yml` | Alias/model; top-level negative test; nested export reconstruction. | Nested expression requires property/field and identity; top-level action rejected. | High | Direct stage action can be invisible in Designer and crash property editing; unknown proxy variants remain unsupported. |
| `lookupListItemIntProperty` | unsupported | `lookupSPListItemIntProperty` | `LookupListItemIntPropertyActionYaml` | YAML shape only; build fails if proxy type missing; top-level rejected. | Not exported. | none | Top-level negative test. | Top-level action rejected. | High | Tested cache lacks `LookupSPListItemIntProperty`; do not publish as visible action. |
| `callHttpWebService` | preview | `callHttp`, `http` | `CallHttpWebServiceActionYaml` | Supported; emits SharePoint `CallHTTPWebService` with request method, optional `requestContent` / `requestHeaders` DynamicValue variable references, and response targets. | Structural export recognizes `CallHTTPWebService`, including request content/header variables and response targets. | `samples/workflow.http.yml`, `samples/workflow.http-post.yml`, complex samples; `artifacts/httpposttest.exported.yml` diagnostics | Alias/model/method validation; request content/header YAML load coverage; WebsiteCache POST sample build. | Requires at least one response target; literal method allow-list. | Medium | For POST/PUT, build request headers/body first with `buildDynamicValue` and reference those variables from `requestHeaders` / `requestContent`; no external HTTP call in automated tests; runtime depends on target service latency, auth, response size, payload shape, and Workflow Manager HTTP limits. |
| `sendEmail` | preview | `email` | `SendEmailActionYaml` | Supported; emits SharePoint `Email`. | Structural export recognition added from checked-in email/reference XAML; recipients placeholdered. | `samples/workflow.email.yml`, complex samples | Alias/model/required `to`. | Requires `to`. | High | Can notify real users; samples use safe placeholder recipients; review recipients/body and tenant mail behavior before live publish. |
| `singleTask` | preview | `task` | `SingleTaskActionYaml` | Supported; emits bounded SharePoint `SingleTask`. | Structural export recognition added when `SingleTask` is present in reference XAML; assignee/title/body are retained when simple attributes. | `samples/workflow.task.yml` | Alias/model/required assignee/title plus structural export recognition. | Requires `assignedTo` and `title`. | High | Defaults waive emails; review assignees, task list behavior, and escalation expectations before live publish. |
| `lookupRestPropertyName` | preview | `lookupSPListItemPropertyNameInREST` | `LookupRestPropertyNameActionYaml` | Supported; emits SharePoint REST property-name lookup. | Not exported. | HTTP/REST samples | Alias/model/required `to`. | Requires `to`. | Medium | Requires proxy metadata at build time and should be tested with the exact REST response shape produced by the target site. |
| `getDynamicValueProperty` | experimental | `getDictionaryItem`, `getDictionaryValue`, `getResponseProperty` | `GetDynamicValuePropertyActionYaml` | Supported for typed extraction from HTTP/manual `DynamicValue`. | Structural export recognizes downloaded/generated `GetDynamicValueProperty` shapes. | HTTP/REST samples; `samples/workflow.experimental-dynamic-values.yml` | Alias/model/required source/to; dynamic array export tests. | Requires `source` and `to`; build validates variable types. | Medium | Supports explicit value type hints for String, Boolean, Int32, Double, DateTime, Guid, and DynamicValue; runtime fails can come from missing/null properties, array/object mismatches, or unexpected REST payloads. |
| `setDynamicValueProperty` | experimental | `setDictionaryItem`, `setDictionaryValue`, `setResponseProperty` | `SetDynamicValuePropertyActionYaml` | Experimental support; emits hidden Microsoft.Activities `SetDynamicValueProperty` to produce/update a `DynamicValue`. | Not exported. | `samples/workflow.experimental-dynamic-values.yml` | Alias/model/required source; build dispatch. | Requires `source`; optional `to` defaults to source; build validates source/target are `DynamicValue`. | High | Hidden Designer action. Manual SharePoint/Designer test confirmed hidden-action execution, but exposed a manual-start metadata follow-up; publish/runtime behavior may differ by proxy version. |
| `buildDynamicValue` | experimental | `buildDictionary`, `createDictionary` | `BuildDynamicValueActionYaml` | Supported; emits Microsoft.Activities `BuildDynamicValue` for request payload/header dictionaries and experimental DynamicValue samples. | Structural export recognizes generated/downloaded `BuildDynamicValue` dictionaries when entries are directly representable as simple arguments or variable references. | `samples/workflow.experimental-dynamic-values.yml`, `samples/workflow.http-post.yml`; `artifacts/httpposttest.exported.yml` diagnostics | Alias/model/required `to`; entry validation; HTTP POST export/build coverage. | Requires `to` and at least one keyed entry; target is inferred as `DynamicValue` if omitted. | Medium | Supports scalar/string/numeric/Boolean/DateTime/Guid entry values and nested `DynamicValue` entries with `valueType: DynamicValue`; structural export is diagnostic and may placeholder or misclassify complex operands that are not simple literals/variables. |
| `buildUri` | dev-only | none | `BuildUriActionYaml` | Dev-only hidden action; emits Microsoft.Activities `BuildUri`, accepting `source` or scalar `scheme`/`host`/`port`/`path`/`query`/`fragment`, writing a `String`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires `to`; target inferred as `String`. | High | Hidden in SharePoint Designer; inspect generated XAML and runtime-test before publishing. |
| `getConfigurationValue` | dev-only | none | `GetConfigurationValueActionYaml` | Dev-only hidden action; emits Microsoft.Activities `GetConfigurationValue`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires `name` and `to`; target inferred as `String`. | High | Hidden in SharePoint Designer; runtime values are environment/proxy-defined. |
| `getInstanceAddress` | dev-only | none | `GetInstanceAddressActionYaml` | Dev-only hidden action; emits Microsoft.Activities `GetInstanceAddress`, writing a `String`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires `to`; target inferred as `String`. | High | Hidden in SharePoint Designer. |
| `setUserStatus` | dev-only | none | `SetUserStatusActionYaml` | Dev-only hidden action; emits Microsoft.Activities `SetUserStatus` with string-expression `description`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Common validation only. | High | Hidden in SharePoint Designer; no result target. |
| `createTimeSpan` | dev-only | none | `CreateTimeSpanActionYaml` | Dev-only hidden action/expression support; emits Microsoft.Activities.Expressions `CreateTimeSpan`, writing a `TimeSpan`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires `to`; target inferred as `TimeSpan`. | High | Hidden in SharePoint Designer; scalar days/hours/minutes/seconds only. |
| `getTimeSpanFields` | dev-only | none | `GetTimeSpanFieldsActionYaml` | Dev-only hidden action; emits Microsoft.Activities.Expressions `GetTimeSpanFields`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires at least one output target. | High | Hidden in SharePoint Designer; scalar output targets only. |
| `addToDate` | dev-only | none | `AddToDateActionYaml` | Dev-only hidden action/expression support; emits Microsoft.Activities.Expressions `AddToDate`, writing a `DateTime`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires `to`; target inferred as `DateTime`. | High | Hidden in SharePoint Designer; supports `timeSpan` or scalar day/hour/minute/second fields. |
| `subtractFromDate` | dev-only | none | `SubtractFromDateActionYaml` | Dev-only hidden action/expression support; emits Microsoft.Activities.Expressions `SubtractFromDate`, writing a `DateTime`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires `to`; target inferred as `DateTime`. | High | Hidden in SharePoint Designer; scalar fields only. |
| `dateInRange` | dev-only | none | `DateInRangeActionYaml` | Dev-only hidden action/expression support; emits Microsoft.Activities.Expressions `DateInRange`, writing a `Boolean`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | Alias/model/build dispatch; WebsiteCache sample build. | Requires `to`; target inferred as `Boolean`. | High | Hidden in SharePoint Designer; scalar DateTime input/start/end only. |
| `toLowerCase` expression | dev-only | `lowerCase`, `toLower` | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `ToLowerCase`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Nested expression only; result must flow to `String`/`Object`. | High | Hidden-expression support. May be invisible or problematic in SharePoint Designer UX; local build success does not prove publish/runtime parity. |
| `toUpperCase` expression | dev-only | `upperCase`, `toUpper` | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `ToUpperCase`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Nested expression only; result must flow to `String`/`Object`. | High | Hidden-expression support. May be invisible or problematic in SharePoint Designer UX; local build success does not prove publish/runtime parity. |
| `stringLength` expression | dev-only | `lengthString` | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `StringLength`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Nested expression only; result must flow to `Int32`/`Object`. | High | Hidden-expression support. May be invisible or problematic in SharePoint Designer UX; local build success does not prove publish/runtime parity. |
| `replaceString` expression | dev-only | `replace` | `ExpressionYaml` | Dev-only nested expression form of Microsoft.Activities.Expressions `ReplaceString`; distinct from the supported top-level assignment action of the same canonical name. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value` plus `pattern`/`oldValue`/`find`; optional `replacement`/`newValue`/`replaceWith`; result must flow to `String`/`Object`. | High | Prefer the top-level `replaceString` action for SPD-friendly workflows; nested form may be hidden/mis-rendered by Designer. |
| `substring` expression | dev-only | `substringString` | `ExpressionYaml` | Dev-only nested expression form of Microsoft.Activities.Expressions `Substring`; distinct from the supported top-level assignment action of the same canonical name. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value`; optional `startIndex` defaults to 0; optional `length`; result must flow to `String`/`Object`. | High | Uses zero-based .NET indexes. Prefer the top-level `substring` action for SPD-friendly workflows; nested form may be hidden/mis-rendered by Designer. |
| `trimString` expression | dev-only | `trim` | `ExpressionYaml` | Dev-only nested expression form; emits Microsoft.Activities.Expressions `Trim`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value`; result must flow to `String`/`Object`. | High | Prefer the top-level `trimString` action for SPD-friendly workflows; nested form may be hidden/mis-rendered by Designer. |
| `indexOfString` expression | dev-only | `indexOf` | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `IndexOfString` when available in the local proxy assembly. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value` and `searchValue`/`pattern`/`find`; result must flow to `Int32`/`Object`. | High | Optional proxy lookup: build fails clearly if the local Microsoft.Activities proxy lacks the type. SharePoint Designer may hide/mis-render it. |
| `isEmptyString` expression | dev-only | `stringIsEmpty` | `ExpressionYaml` | Dev-only nested predicate; emits Microsoft.Activities.Expressions `IsEmptyString` when available in the local proxy assembly. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value`; result must flow to `Boolean`/`Object`. | High | Optional proxy lookup: build fails clearly if the local Microsoft.Activities proxy lacks the type. SharePoint Designer may hide/mis-render it. |
| `containsString` expression | dev-only | `contains` | `ExpressionYaml` | Dev-only nested predicate; emits Microsoft.Activities.Expressions `ContainsString`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value` and `searchValue`/`pattern`/`find`; result must flow to `Boolean`/`Object`; `valueType: StringIgnoreCase` sets `IgnoreCase` when writable. | High | The same proxy type is also used for `if`/`while` string conditions; nested expression assignment remains dev-only/Designer-risky. |
| `startsWithString` expression | dev-only | `startsWith` | `ExpressionYaml` | Dev-only nested predicate; emits Microsoft.Activities.Expressions `StartsWithString`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value` and `searchValue`/`pattern`/`find`; result must flow to `Boolean`/`Object`; `valueType: StringIgnoreCase` sets `IgnoreCase` when writable. | High | The same proxy type is also used for `if`/`while` string conditions; nested expression assignment remains dev-only/Designer-risky. |
| `endsWithString` expression | dev-only | `endsWith` | `ExpressionYaml` | Dev-only nested predicate; emits Microsoft.Activities.Expressions `EndsWithString`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires `value` and `searchValue`/`pattern`/`find`; result must flow to `Boolean`/`Object`; `valueType: StringIgnoreCase` sets `IgnoreCase` when writable. | High | The same proxy type is also used for `if`/`while` string conditions; nested expression assignment remains dev-only/Designer-risky. |
| `parseBoolean` expression | dev-only | `parseBool` | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `ParseBoolean` when available in the local proxy assembly. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage when cache integration is enabled. | Requires string `value`; result must flow to `Boolean`/`Object`. | High | Optional proxy lookup: build fails clearly if the local Microsoft.Activities proxy lacks the type. Invalid Boolean strings fail at runtime/proxy evaluation. |
| `concatString` expression | dev-only | `concat` | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `ConcatString`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Requires `value` or `values`; result must flow to `String`/`Object`. | High | Hidden-expression support. May be invisible or problematic in SharePoint Designer UX; local build success does not prove publish/runtime parity. |
| `currentDate` expression | dev-only | none | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `CurrentDate`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Nested expression only; result must flow to `DateTime`/`Object`. | High | Runtime timezone/locale behavior is proxy-defined; SharePoint Designer may hide or mis-render the node. |
| `newGuid` expression | dev-only | none | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `NewGuid`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Nested expression only; result must flow to `Guid`/`Object`. | High | Hidden-expression support. SharePoint Designer may hide or mis-render the node. |
| `parseGuid` expression | dev-only | none | `ExpressionYaml` | Dev-only nested expression; emits Microsoft.Activities.Expressions `ParseGuid`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing coverage; available to WebsiteCache build path when used. | Requires `value`; result must flow to `Guid`/`Object`. | High | Invalid Guid strings fail at runtime/proxy evaluation. SharePoint Designer may hide or mis-render the node. |
| `containsDynamicValueProperty` expression | dev-only | `containsDictionaryProperty` | `ExpressionYaml` | Dev-only nested predicate; emits Microsoft.Activities `ContainsDynamicValueProperty`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Requires DynamicValue `source`/`value` and `propertyName`; result must flow to `Boolean`/`Object`. | High | Hidden Microsoft.Activities support. May cause SharePoint Designer UX issues; local build success does not prove publish/runtime parity. |
| `isEmptyDynamicValue` expression | dev-only | `isEmptyDictionary` | `ExpressionYaml` | Dev-only nested predicate; emits Microsoft.Activities `IsEmptyDynamicValue`. | Not exported. | `samples/workflow.devonly-microsoft-activities-expressions.yml` | YAML parsing plus WebsiteCache build/XAML emission coverage. | Requires DynamicValue `source`/`value`; result must flow to `Boolean`/`Object`. | High | Hidden Microsoft.Activities support. May cause SharePoint Designer UX issues; local build success does not prove publish/runtime parity. |
| `ClearDictionary` hidden action | unsupported | none | none | Not implemented. Current YAML variable model represents `DynamicValue`; the inspected `ClearDictionary<TKey,TValue>` action requires an `IDictionary<TKey,TValue>` variable/action surface. | Not exported. | `samples/workflow.experimental-dictionary-clear-notes.yml` | Safe limitation sample build. | Not applicable. | High | Left as a documented limitation to avoid adding broad IDictionary support in this subtask. |

## Experimental DynamicValue manual test notes

## HTTP request header/body guidance

Use `callHttpWebService` for HTTP GET/POST/PUT/DELETE calls. Literal `requestType` values can be the short method names (`GET`, `POST`, `PUT`, `DELETE`) or SharePoint Designer names (`HTTPGET`, `HTTPPOST`, `HTTPPUT`, `HTTPDELETE`); YAML build normalizes short names to the SharePoint Designer form.

For GET-style calls, `requestContent` and `requestHeaders` can be omitted. The builder supplies SharePoint Designer-compatible placeholder `DynamicValue` references so the `CallHTTPWebService` activity renders cleanly.

For POST/PUT calls with headers or a body:

1. Declare the request header/body variables as `DynamicValue`.
2. Build each dictionary with `buildDynamicValue` before the HTTP call.
3. Set `requestHeaders` to the header dictionary variable name.
4. Set `requestContent` to the body dictionary variable name.
5. Use `responseStatusCodeTo`, `responseContentTo`, and/or `responseHeadersTo` for outputs. Response content/header variables are inferred as `DynamicValue` when omitted from the `variables` list.

Minimal POST pattern:

```yaml
variables:
  - name: requestHeaders
    type: DynamicValue
  - name: requestBody
    type: DynamicValue
  - name: httpStatusCode
    type: String
stages:
  - name: POST
    actions:
      - type: buildDynamicValue
        to: requestHeaders
        entries:
          - key: Accept
            value: application/json
          - key: Content-Type
            value: application/json
      - type: buildDynamicValue
        to: requestBody
        entries:
          - key: Title
            value: Example title
      - type: callHttpWebService
        address: http://example.com
        requestType: POST
        requestHeaders: requestHeaders
        requestContent: requestBody
        responseStatusCodeTo: httpStatusCode
        responseContentTo: responseContent
        responseHeadersTo: responseHeaders
```

Nested body dictionaries are supported, but the nested entry must opt into `DynamicValue` serialization:

```yaml
- type: buildDynamicValue
  to: childPayload
  entries:
    - key: childProp
      value: child value
- type: buildDynamicValue
  to: requestBody
  entries:
    - key: Child
      value:
        variable: childPayload
      valueType: DynamicValue
```

Operational caveats:

- `requestHeaders` / `requestContent` are variable names, not inline dictionaries.
- Keep payloads small and explicitly shaped; large or unpredictable `DynamicValue` payloads can fail publish/runtime persistence or become hard to inspect in SharePoint Designer.
- Validate external endpoint latency, auth expectations, status codes, and response payload shape in a non-production SharePoint site before production use.
- `samples/workflow.http-post.yml` is the reference YAML sample. The downloaded SharePoint Designer workflow `httpposttest` exported to `artifacts/httpposttest.exported.yml` confirms the supported POST/header/body shape from live XAML.

2026-05-11 manual SharePoint/SharePoint Designer test: `samples/workflow.experimental-dynamic-values.yml` published with manual start enabled, and the hidden `Microsoft.Activities` actions were not visible in SharePoint Designer. SharePoint Designer did not report validation errors for those hidden activities, preserved them when re-publishing, and a site-triggered run wrote workflow history output including `Set by hidden Microsoft.Activities.SetDynamicValueProperty`. This confirms the current hidden-action approach can emit extra `Microsoft.Activities` namespace actions that SharePoint Designer does not show visually, and those hidden actions can still execute successfully after a Designer re-publish.

Known follow-up: manual start attempted to launch an initializer form instead of starting directly. This suggests the generated publish metadata may be advertising initiation parameters/form fields even though none are intended. Treat this as a metadata/form-start issue to fix separately, not as a blocker for hidden `Microsoft.Activities` action emission or execution.

The dev-only expression/action batch remains intentionally narrow: string casing/search/predicate/manipulation helpers, Boolean parsing, current date, new/parsed Guid, DynamicValue predicates, Batch 1 simple hidden actions (`buildUri`, `getConfigurationValue`, `getInstanceAddress`, `setUserStatus`), and contained TimeSpan/date helpers (`createTimeSpan`, `getTimeSpanFields`, `addToDate`, `subtractFromDate`, `dateInRange`). Broader Boolean expression assignment helpers (`not`, `and`, `or`), `createDate`/`getDateFields`/elapsed-date helpers, generic dictionaries/collections, cache scope, split arrays, suspend/track/runtime-control actions, and broad arithmetic/parse generics remain deferred.

## Export parity notes

`ExportWorkflowYaml` is a lightweight diagnostic export, not a guaranteed full XAML-to-YAML round trip. When WebsiteCache/config is available, export first attempts WF object-model deserialization and can reconstruct stage ordering and non-linear transitions from `System.Activities.Statements.Flowchart`, `FlowStep.Next`, and `FlowDecision.True` / `FlowDecision.False` object references. This cache-enabled path is preferred for downloaded or published workflows that use explicit stage transitions. If object-model export is unavailable, XML structural export recognizes common generated/reference XAML shapes and can still produce diagnostic YAML, but transition and expression fidelity is lower.

The exporter recognizes direct SharePoint child activities that are already supported by YAML-to-XAML and now has reference-backed recognition for common generated/reference XAML shapes including `Comment`, delays, `SetField`, list lifecycle actions, `Email`, `CallHTTPWebService`, `SingleTask`, plain WF `Assign`, WF `While`/`If` containers, generated numeric/string/Boolean/DateTime/DynamicValue comparison expressions, nested `and`/`or`/`not` condition trees, `parseDate`, `parseDynamicValue`, generated string-manipulation expression activities for `ReplaceString`, `Substring`, and `Trim`, and narrowly recognized nested lookup expression activities. The exporter preserves supported nested actions under `while`/`if`, normalizes SharePoint Designer-local DateTime operands to UTC, retains condition/expression Designer `Id` values as `designerId` when required for RHS operand rendering, exports terminal stage references as `goto: end`, and emits buildable placeholder comparison conditions when it cannot reconstruct the exact condition safely. Expressions, recipients, list IDs, identities, and operands are intentionally placeholdered unless represented as simple attributes, simple literals/variables, or narrowly recognized generated/VB expression text such as current-list/current-item/context/list-item lookups. Most generated workflows are wrapped in SharePoint Designer stage metadata, nested WF `InArgument` expressions, and proxy-specific activity shapes; adding robust export for the full action surface requires either WF deserialization with the local WebsiteCache proxy assemblies or more detailed proxy metadata than this repository can safely assume. Export warnings remain explicit so partial diagnostic output is not mistaken for authoritative round-trip YAML.

## SharePoint Designer conditional metadata notes

The `ExampleConditionals` round-trip showed that publish validation and Designer rendering are separate compatibility bars. A generated condition tree can validate and publish while Designer still displays `(insert a condition)` or blank RHS operands if the expression metadata is not Designer-compatible. Keep these rules when changing condition builders/exporters:

- Boolean condition and operand conversion expression nodes need SPD-compatible `Result="{x:Null}"` plus the expected Designer custom attributes.
- `ParseDate.CultureName` should remain the SharePoint Designer property-element shape that calls `GetConfigurationValue`; do not flatten it to a plain culture attribute when preserving SPD-authored conditions.
- RHS `ArgumentValue` condition operands need `ArgumentValue.Result` / `OutArgument` wrappers for Designer token rendering.
- Nested RHS conversions such as `ParseDate` and `ParseDynamicValue` may need the original Designer `Id` GUID preserved through YAML as `designerId`; the live `ExampleConditionalsRoundTripPreserveIds4` validation confirmed missing RHS values became visible after preserving those IDs.
- SharePoint expression proxy namespaces should use the SPD/publish-compatible non-assembly namespace form.

Any mention of VB expression text in export support means legacy/import compatibility only. New builders and generated XAML must continue to emit structured activity nodes and must reject raw VB/C# WF language expression activities before publishing.

## Adding a new action checklist

Keep action additions narrow and make each supported action explicit across the YAML, build, export, and test surfaces:

- Add or extend the YAML model in `YamlWorkflowModel.cs`, including a stable canonical `Type` value and any safe aliases in `WorkflowActionYamlTypeConverter`.
- Add validation near the model for required fields, safe top-level versus nested-expression usage, and any allow-lists that prevent unsupported Designer/runtime shapes.
- Update `WorkflowVariableTypeInferer` when the action writes to workflow variables or requires implicit infrastructure variables.
- Implement the activity builder in the appropriate file under `Actions/`, reusing `WorkflowExpressionBuilder` and preserving existing expression behavior.
- Do not implement builders with raw WF language expression activities (`VisualBasicValue`, `VisualBasicReference`, `CSharpValue`, or `CSharpReference`). Use structured SharePoint proxy or `Microsoft.Activities.Expressions` nodes and add tests/guards proving the generated XAML avoids raw language expression types.
- Keep publish metadata in YAML and ensure the effective metadata sidecar `*.xaml.metadata.json` remains authoritative for display name, technical name, description, target, start options, initiation settings, and form fields. Do not add a normal publish dependency on `*.xaml.formfield.xml`; FormField XML must remain deprecated fallback/compatibility output only.
- Register the action model type in `WfActivityBuilderSerializer.CreateActionBuilders()` so dispatch is explicit and discoverable.
- Decide whether the action is valid as a nested expression, a stage action, or both; document and test the expression behavior.
- For condition/expression builders, preserve Designer-compatible metadata shapes (`Result`, custom attributes, property-element configuration values, `ArgumentValue.Result` wrappers, `designerId` when exported) before treating publish validation as sufficient.
- Update `WorkflowYamlExporter` only if structural export can recognize the action safely; otherwise mark export as unsupported in this matrix.
- Add or update tests for aliases/model conversion, validation failures, build-dispatch registration, variable inference, and export recognition when applicable.
- Add a safe sample YAML file or extend an existing sample only when the action can be demonstrated without live side effects.
- Add or update the support-matrix row with aliases, builder status, export status, sample/test coverage, validation status, risk, and caveats.
- If the change affects stage flow, update `docs/stage-transitions.md` and validate cache-enabled export against `Flowchart` / `FlowStep` / `FlowDecision` object references as well as structural XML fallback.

## Repository structure note

The solution file `SPNet.slnx` includes only the serializer and CSOM publisher projects, plus the test project added under `tests/`. If IDE state shows `src/SPNet.Workflow.Core/`, `src/SPNet.Workflow.Cli/`, or `src/SPNet.Workflow.SharePoint/`, those are stale visible tabs or files from another workspace state and are not part of this repository snapshot.
